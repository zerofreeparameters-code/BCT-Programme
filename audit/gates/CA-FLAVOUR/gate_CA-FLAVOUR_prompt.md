# Gate CA-FLAVOUR — pre-registration prompt

**Run in a FRESH thread with fresh context (Whopper Protocol). One gate, that thread only. Hash-before-read. Adversarial: one-sentence-kill rule applies; null results are findings. CONJECTURE-capped if run same-session as any other gate. Terminal-verify every load-bearing claim (mpmath 60 dps work / 40 dps report; lattice/point-group facts computed, not asserted).**

## Question

At **c/a = √2** — the ratio the whole programme is built on — the BCT lattice is **FCC** (cubic, point group O_h, order 48), and the tetrahedral void is a **regular** tetrahedron with **full, unbroken T_d** site symmetry. Every ingredient a nonzero quark **mixing** texture needs (a D-type residual with texture zeros, a privileged c-axis to host the r_oct/r_tet "z-weight" that would give V_cb, a c-axis chirality to carry the CP phase, and a *split* — non-degenerate — generation triplet) requires the tet void's T_d to be **tetragonally broken**, i.e. c/a ≠ √2.

**Does c/a = √2 therefore FORBID a nonzero quark mixing texture (V_CKM ≠ 𝟙, J ≠ 0) as a zero-parameter geometric output — so that the flavour sector (needs c/a ≠ √2) and the α-geometry sector (needs c/a = √2 for α₀ = r_oct·r_tet/π and the uniqueness theorem) demand *incompatible* values of c/a and cannot both be derived from one lattice at one ratio?**

If yes, THE KNOT is not an unsolved construction — it is **structurally excluded at √2**, and the flavour gap is the shadow of c/a = √2 being the point of maximal symmetry. If no (an escape exists), THE KNOT reopens as constructible and the escape must be exhibited with zero free parameters and no OP/R2/P obstruction.

## Background (established — do not re-derive; reconcile against, after forming an independent read)

- **Gate TEXTURE (SC-TEXTURE-1):** BCT has no single derived quark mass matrix; masses, CKM, CP phase, arg det are separately fitted/imported. THE KNOT.
- **Gate KND:** at c/a = √2 the NN shell is **12 bonds, not 8**; the bond graph is **not bipartite** (explicit triangle, C_A(3)=48); the true point group is **O_h (order 48)**; the 8-bond truncation *manufactures* the tetragonal anisotropy the corpus attributes to the lattice (CN-KND-07); the nodal-manifold irreducible-component count is **1, not 3**, so App Q's three generations "rest on nothing in the corpus" (CN-KND-10: 3 void sites but only 2 Wyckoff orbits). Blast radius spared the **numeric mass chain** (magnitudes), not the qualitative flavour structure. §5.1 forbids treating "there must be three sectors" as a constraint.
- **Gate FWD (CN-FWD-09):** r_oct and r_tet are the interstitial radii of **every** close packing (FCC oct/tet holes); the α match is not evidence for BCT specifically, and the c/a = √2 uniqueness theorem "does no work in the α derivation." A number from the geometric alphabet landing near a target carries no evidential weight (113-expressions trap).
- **Gates OP / R2 / P:** no consistent symmetry-breaking order parameter was found for the gravity sector across the enumerated classes (E_g⊕T₂g soft multiplet, fracton/tensor-gauge) — any *dynamical* O_h → T_d breaking proposed as a flavour escape inherits this territory.
- **Crystallographic fact (verify, do not assume):** body-centred tetragonal with c/a = √2 **is** FCC. Just below √2 the shell is 8 (BCC-type, one privileged axis); just above, 4; only at √2 is it 12 (cubic). Tetragonal anisotropy exists **only** away from √2.
- The corpus's own commitments to check: the tet void is assigned **T_d** (App AE/AU/AP/T); generations are assigned to the **T₂ triplet** (App AP); α₀ and the void radii are evaluated **at c/a = √2** (App/Monograph FWD sections).

## The things it must resolve (any one unresolved in favour of the texture ⇒ SC-CAFLAV-3; all confirmed ⇒ SC-CAFLAV-1)

1. **Symmetry at √2 (load-bearing).** Compute, cold, the tet-void point symmetry and the lattice NN shell at c/a = √2 and at √2 ± ε. **One-sentence kill:** if the shell at √2 is 12 cubic bonds (O_h, order 48) and the tetrahedral interstitial is a *regular* tetrahedron with full T_d (−43m) site symmetry, then there is no privileged c-axis and no tetragonal remnant at that ratio, and the flavour scaffolding (D-residual, z-weight, c-axis chirality) provably requires c/a ≠ √2.

2. **Degeneracy of the triplet under unbroken T_d.** **One-sentence kill:** since T₂ is *irreducible* under T_d, unbroken T_d forces the three generations to be exactly degenerate ⇒ V_CKM = 𝟙 and J = 0 with **no free parameter available to lift them** ⇒ any nonzero mixing requires breaking T_d, i.e. c/a ≠ √2. (Do not accept a "small breaking" that is itself an undeclared free parameter.)

3. **The incompatibility.** Confirm that α₀ = r_oct·r_tet/π and the uniqueness/densest-packing claim are evaluated *at* c/a = √2. **One-sentence kill:** if the geometry sector requires c/a = √2 exactly while a nonzero texture requires c/a ≠ √2, the two sectors demand incompatible c/a and cannot be simultaneously derived from one lattice at one ratio — THE KNOT is structurally excluded at √2.

4. **Escape routes (enumerate and evaluate each; do not wave away).**
   - (i) *Dynamical* O_h → T_d breaking by a matter order parameter at c/a = √2: this is a NEW order-parameter assumption and inherits Gates OP/R2/P (no consistent SB order parameter found) — a *derived*, obstruction-free order parameter must be exhibited, or this is not zero-parameter.
   - (ii) *Radial/instanton* generation labels: if the orbit integers n_q are radial/instanton quantum numbers (not T_d generation labels), the mass **magnitudes** may survive at √2 (consistent with KND sparing the numeric chain) while **mixing** still requires broken T_d — report exactly which flavour numbers survive and which don't.
   - (iii) A frozen c/a ≠ √2 vacuum: forfeits α₀ and uniqueness — report the trade explicitly.
   - **Requirement:** any claimed escape must reproduce V_us, V_cb, V_ub, J with zero free inputs beyond geometry AND survive the OP/R2/P obstruction it inherits; otherwise it is SC-CAFLAV-2/1, not 3.

## Hash-before-read targets (shasum -a 256; confirm before reading)

- KND result / its appendices: **App K, N, Q, R, T, U, V, AD** (and one section each of S, AE) — in Appendices Part1/Part2 (plain text despite `.pdf`).
- Flavour texture appendices: **App AE, AU, AP, T** (the four the TEXTURE build examined).
- α / c/a = √2: the FWD sections defining α₀ = r_oct·r_tet/π and the c/a = √2 uniqueness theorem (Monograph + appendices).
- Expected digests from the 16 Sep TEXTURE session (cross-check only; recompute locally):
  - Appendices Part1 `5656f76387be0aa9f00bef574e5d99de0635acb49eb66122b135d06b821548c7`
  - Appendices Part2 `7a09746433339a1619ff450c5427af628dd39e0e94e3573c7271c55a5b270a54`
  - Appendices Part3 `27f4c8b101e2aa34e41a2633304d08032e42d9fc380f1a01d76f71f4f100bde6`
  - Monograph v21 `b057829629c9fc31f3aafea1eaec427299f1bcf9266b86665e116dfd7ea67f69`
  - NPBS2601701_3 `03db3b10c6e989ef3d79d033db3fc392a879e66748deeeaa4c90fb6f5f5b3d47`

## Guards

- Do NOT accept "the tet void has T_d" as evidence of tetragonal *breaking*: T_d is the site symmetry of a *regular* tetrahedral interstitial in cubic FCC — it is **unbroken** at √2, not a broken remnant.
- Do NOT read "c/a = √2 is special" as "c/a = √2 breaks symmetry": it is special as the point of **maximal** (cubic) symmetry / densest packing — the opposite of a breaking.
- Do NOT let a proximity match (e.g. V_cb·m_b/m_s ≈ r_oct/r_tet) count as a mechanism once the privileged axis is removed (CN-FWD-09 trap).
- Do NOT treat "there must be three generations / three sectors" as a constraint (KND §5.1).
- Terminal-verify the NN shell and point-group order at √2 and √2 ± ε (reproduce 12 → 8/4 split; O_h order 48 = 48 signed permutation matrices).
- Do NOT dispute the void-geometry **numeric** layer as packing facts (r_oct, r_tet as FCC interstitials): the gate tests whether a flavour *mixing* texture can **coexist** with c/a = √2, not the α number itself.

## Expected outcomes (all are findings)

- **SC-CAFLAV-1 (most likely):** c/a = √2 is cubic O_h / unbroken T_d ⇒ no privileged axis, degenerate T₂ triplet ⇒ nonzero quark mixing (V_CKM ≠ 𝟙, J ≠ 0) is a zero-parameter *impossibility* at that ratio ⇒ flavour and α-geometry demand incompatible c/a ⇒ **THE KNOT is structurally excluded at √2, not merely unsolved.** Consolidates TEXTURE + HERM + EDM + THETA + MIX + KND into one structural statement.
- **SC-CAFLAV-2:** mass **magnitudes** survive at √2 (radial/instanton orbit labels) but **mixing** does not; deliver the exact survive/fail split of flavour observables.
- **SC-CAFLAV-3 (the escape / the prize):** a *derived*, zero-parameter, OP/R2/P-obstruction-free mechanism produces nonzero mixing at c/a = √2 without a separate tetragonal distortion ⇒ the incompatibility is evaded ⇒ THE KNOT reopens as constructible. Triple-check under fresh brain, terminal-verified, before any tier movement — this would be the largest positive result in the flavour sector.

## Standing note

The void-geometry numeric layer (α₀ as a packing relation, r_oct, r_tet, ξ, the percolating network, the isotropic acoustic metric) is not refuted by this gate regardless of outcome. The gate tests the **coexistence** of a nonzero flavour mixing texture with c/a = √2 — not the geometry itself.

---
*As-run note: prompt arrived as an in-context chat document (paste), not a curl-to-disk handoff, so there is no authoritative pre-registration digest for this run. Verdict SC-CAFLAV-1; all load-bearing crystallography and group theory re-derived cold in-session (see deliverable + caflav_1..4 scripts).*
